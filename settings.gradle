include 'boot-common', 'metrics-prometheus', 'events', 'error-reporting', 'webflux-common', 'webflux-swagger', 'webflux-swagger-ui', 'webflux-error', 'tracing', 'tracing-jaeger'

rootProject.name = 'springboot'
rootProject.children.each {
    it.name = rootProject.name + "-" + it.name
}
