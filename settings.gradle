include 'boot-common',
        'auth-jwt',
        'log',
        'metric',
        'metric-prometheus',
        'metric-collector-api',
        'metric-collector-camunda',
        'events',
        'error',
        'error-reporting',
        'webflux-common',
        'webflux-swagger',
        'webflux-swagger-ui',
        'webflux-error',
        'tracing',
        'tracing-jaeger',
        'job-starter',
        'service-starter'

rootProject.name = 'springboot-lib'
rootProject.children.each {
    it.name = rootProject.name + "-" + it.name
}
