include 'webflux-swagger', 'webflux-swagger-ui', 'webflux-common', 'events', 'webflux-error', 'webflux-tracing', 'webflux-tracing-jaeger'

rootProject.name = 'springboot'
rootProject.children.each {
    it.name = rootProject.name + "-" + it.name
}
